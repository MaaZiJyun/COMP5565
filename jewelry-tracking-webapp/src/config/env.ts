const config = {
    APPLICATION_NAME: process.env.NEXT_PUBLIC_APPLICATION_NAME || 'Default App Name',
    ROOT_API: process.env.NEXT_PUBLIC_ROOT_API || 'http://localhost:3000',
  };
  
  export default config;
  